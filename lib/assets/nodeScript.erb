class node_<%= replace_dashes_with_underscores vm_name %> {
  $admins = [<%= generate_user_array admins %>]
  $users = [<%= generate_user_array users %>]

  realize(Accounts::Virtual[$admins], Accounts::Sudoroot[$admins])
  realize(Accounts::Virtual[$users])
}
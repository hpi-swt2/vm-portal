<%= form_with(model: operation_system, local: true) do |form| %>
  <% if operation_system.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(operation_system.errors.count, "error") %> prohibited this operation_system from being saved:</h2>

      <ul>
      <% operation_system.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

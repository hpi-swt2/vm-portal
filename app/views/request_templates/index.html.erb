<p id="notice"><%= notice %></p>

<h1>Request Templates</h1>

<table class="table">
  <thead>
    <tr>
      <th>Template name</th>
      <th>Cpu count</th>
      <th>Ram mb</th>
      <th>Storage mb</th>
      <th>Operating system</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @request_templates.each do |request_template| %>
      <tr>
        <td><%= request_template.name %></td>
        <td><%= request_template.cpu_count %></td>
        <td><%= request_template.ram_mb %></td>
        <td><%= request_template.storage_mb %></td>
        <td><%= request_template.operating_system %></td>
        <td><%= link_to 'Show', request_template %></td>
        <td><%= link_to 'Edit', edit_request_template_path(request_template) %></td>
        <td><%= link_to 'Destroy', request_template, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Request Template', new_request_template_path, class: 'btn btn-primary' %>

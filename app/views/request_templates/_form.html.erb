<%= form_with(model: request_template, local: true) do |form| %>
  <% if request_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(request_template.errors.count, "error") %> prohibited this request_template from being saved:</h2>

      <ul>
      <% request_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cpu_count %>
    <%= form.number_field :cpu_count %>
  </div>

  <div class="field">
    <%= form.label :ram_mb %>
    <%= form.number_field :ram_mb %>
  </div>

  <div class="field">
    <%= form.label :storage_mb %>
    <%= form.number_field :storage_mb %>
  </div>

  <div class="field">
    <%= form.label :operating_system %>
    <%= form.text_field :operating_system %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%if current_user.admin? && vm.archivable? %>
  <%= link_to fa_icon('archive'),
              {controller: :vms, action: 'archive_vm', id: vm.name},
              method: :post,
              data: { confirm: "Are you sure you want to archive '#{vm.name}'?",
                      toggle: 'tooltip', placement: 'top' },
              title: "Archive VM",
              class: "btn btn-primary btn-sm" %>
<% end %>
<%= link_to fa_icon('arrow-circle-up'),
            {controller: :vms, action: 'revive_vm', id: vm.name},
            method: :post,
            data: { confirm: "Are you sure you want to revive '#{vm.name}'?",
                    toggle: 'tooltip', placement: 'top' },
            title: "Revive VM",
            class: "btn btn-primary btn-sm" %>
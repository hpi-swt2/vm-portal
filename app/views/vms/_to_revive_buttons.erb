<%if current_user.admin? && vm.archivable? %>
  <%= link_to '',
              {controller: :vms, action: 'revive_vm', id: vm.name}, method: :post,
              class: "btn btn-primary btn-sm float-right checkmark" %>
<% end %>